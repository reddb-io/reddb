# Artifact Sizes

Placeholder. Run `scripts/artifact-size.sh` to populate.

PLAN.md B2 gate targets:
- Static Linux binary < 30 MB
- Container image < 50 MB

CI should upload this file as a release artifact after measuring both rows.
