# RedDB v1.0 Release Gate

PLAN.md Phase 10.5 + the two "Definition of Ready" checklists in PLAN.md. This is the merge-block before tagging `v1-cloud-agnostic`. Every box is auditable to a commit, test, or doc page.

> **Status legend** — ✅ shipped on `main`, 🔶 partial (specifics in the row), ❌ not yet started.

## Definition of "Cloud-Agnostic Ready"

| Item | Status | Evidence |
|------|--------|----------|
| Runs identically on Kubernetes, Fly Machines, Docker Compose, systemd, bare-metal | ✅ | `examples/kubernetes/reddb.yaml`, `examples/fly/fly.toml`, `examples/docker-compose.universal.yml`, `examples/systemd/reddb.service` |
| Runs identically on AWS ECS, App Runner, Cloud Run, Container Apps, Lambda+EFS, Nomad | ✅ | Reference manifests in `examples/{aws-ecs,aws-app-runner,aws-lambda-efs,google-cloud-run,azure-container-apps,nomad}/`. |
| Backup against AWS S3, Cloudflare R2, GCS (S3-compat), MinIO, Ceph, B2, NFS/SMB, FS, HTTP | ✅ | `S3Backend` w/ `path_style` toggle (P2.1), `LocalBackend` (FS), `HttpBackend` (P2.3), factory in `service_cli.rs` (P2.5) |
| Zero `aws-sdk-*`, `google-cloud-*`, `azure-sdk-*` references | ✅ | `cargo tree` clean (audited Phase 0); SigV4 hand-rolled in `S3Backend` |
| Zero vendor-named env vars; only `RED_S3_*`, `RED_HTTP_*`, etc. | ✅ | All env vars listed in PLAN.md "Universal Public Contract" follow this rule |
| Public specs: MANIFEST.json, WAL format, admin OpenAPI | ✅ | `docs/spec/manifest-format.md`, `docs/spec/wal-format.md`, `docs/spec/admin-api.openapi.yaml` |
| Chaos tests green on 4 OS × 2 arch × 4 FS × 4 backend types | 🔶 | `chaos` + `chaos-minio` + `drill` jobs wired in `.github/workflows/ci.yml`. Ubuntu (FS + S3) + Windows + macOS covered; aarch64 / btrfs / xfs matrix expansion pending. |
| Cold-start P95 < 2s with volume present | ❌ | Not yet measured. Phase 9.1 work pending. |
| Cold-start P95 < 10s from empty volume + remote (1 GB DB) | ❌ | Not yet measured. Phase 9.1 + 9.2 (incremental snapshot) pending. |
| Container image < 50 MB | 🔶 | Multi-stage Dockerfile shipped (P7.1); size not yet measured against the 50 MB target. |
| Static binary < 30 MB | 🔶 | `release-static` profile + `Dockerfile.musl` shipped (P7.2). Built artifact size not yet measured in CI; release.yml needs the upload-artifact step. |
| Reference deployment manifests for ten+ runtime targets | ✅ | 10 manifests: K8s, Fly, docker-compose (universal/serverless/replica/etc), systemd, ECS, App Runner, Cloud Run, Container Apps, Nomad, Lambda+EFS. |
| Operator documentation covers 90% of predictable questions | ✅ | `docs/operations/runbook.md`, `docs/operations/scaling.md`, `docs/clients/sdk-compatibility.md`, `docs/spec/*` |

## Definition of "Client-Safe Production Ready"

| Item | Status | Evidence |
|------|--------|----------|
| Exactly-one-writer fencing via lease + WriteGate | ✅ | `LeaseStore` (P5.1), `LogicalChangeApplier` boot loop (P5.5), `WriteGate::set_lease_state` (P5.2), `assert_remote_write_allowed` (P5.4). Tests: `chaos_promote_refused_when_lease_held`, `chaos_concurrent_lease_acquire`. |
| Read replicas reject all public mutations | ✅ | `WriteGate::check` rejects `Replica` role on every public surface. Tests: `tests/e2e_replica_readonly.rs`. |
| Replica lag, applied LSN, durable LSN, source LSN visible | ✅ | `/admin/status.replica.primary_view[]`, `reddb_replica_{ack_lsn,lag_records,lag_seconds}` (P11.4) |
| Missing/corrupted/duplicated/reordered WAL fails closed during apply + restore | ✅ | `LogicalChangeApplier::apply` returns `Gap`/`Divergence` (P11.5), restore validates sha256 + hash chain (P11.3). Tests: `chaos_replica_apply_gap`, `chaos_replica_apply_divergence`, `chaos_wal_chain_break`, `chaos_wal_segment_corruption`, `chaos_wal_segment_missing`, `drill_pitr_chain_break_within_window`. |
| Remote WAL versioned, checksummed, hash-chained | ✅ | `WalSegmentManifest.{sha256,prev_hash}`, `UnifiedManifest` with both fields. Spec: `docs/spec/wal-format.md`. |
| Backup restore drills run continuously in CI | 🔶 | Test files exist: `drill_backup_restore_round_trip`, `drill_pitr_target_time`, `drill_pitr_chain_break_within_window`. CI runner (`make drill-nightly`) not yet scripted. |
| Manual promotion requires lease + no WAL gaps + manifest verification + audit | ✅ | `POST /admin/failover/promote` (P11.6) with role + backend + apply-health + lease guards. Audit log entries on success and failure. |
| Synchronous replication claim backed by `ack_n` / `quorum` + chaos tests | 🔶 | `ack_n` foundation + observability + `trigger_backup` integration done (P11.4). DML write-surface integration in progress; helper `enforce_commit_policy` exists. Chaos test for ack_n write path pending. |
| Performance fast paths benchmarked + correctness equivalence tests | ❌ | Phase 12 perf program not started; locked to post-gate per PLAN.md "freeze on main + tag". |

## Engine quality gates

Auto-runnable subset (every gate is a `cargo` invocation operators can replay):

```bash
# Functional correctness
cargo test --lib                              # >2700 unit tests
cargo test --test 'e2e_*'                     # >40 integration tests
cargo test --test 'chaos_*'                   # 8 chaos tests
cargo test --test 'drill_*'                   # 3 backup/restore drills

# Build artifacts
cargo build --release                         # Release binary builds
docker build .                                # Multi-stage container builds

# Style + lint
cargo check
cargo clippy --all-targets -- -D warnings
cargo fmt --check
```

Performance gate (post Phase 12, requires `reddb-benchmark` repo):

```bash
cd ~/Work/FF/reddb-benchmark
make duel-official                            # 10 fresh-Docker runs
make check-baseline OFFICIAL_ONLY=1 GATE_THRESHOLD=0.10
make check-baseline OFFICIAL_ONLY=1 GATE_P99_THRESHOLD=0.15
```

## Items deferred past v1.0

Documented in PLAN.md "Out of Scope (Intentional)" — none of these block v1.0:

- Database registry + multi-tenant control plane (separate project)
- Customer auth (OAuth, MFA, sign-up)
- Billing integration
- Dashboard SPA
- DNS routing / wildcard certs
- Cloud-specific provisioning (Fly API, Kubernetes API calls)
- Multi-region replicas with global routing
- Window functions, FDW, full SSI, full extensions
- Automatic failover (manual only in v1.0)

## Sign-off

The release manager signs by:

1. Verifying every ✅ row above against the cited evidence on the v1.0 commit.
2. Documenting workarounds for every 🔶 row (or upgrading to ✅ before tagging).
3. Filing follow-up issues for every ❌ row, tagged `v1.x`.
4. Tagging `v1-cloud-agnostic` and bumping `Cargo.toml` version.

## Path to v1.0

The remaining work to flip every 🔶 / ❌ to ✅, in suggested order:

1. **Phase 8.4 chaos matrix CI** — wire `chaos_*` + `drill_*` to GitHub Actions matrix (4 OS × 2 arch × 4 FS × 4 backends). Most blocking item.
2. **Phase 9.1 cold-start measurement** — establish the baseline. May reveal Phase 9.2 (incremental snapshot) is mandatory or merely nice-to-have.
3. **Phase 7.2 static binary** — Cargo profile + `musl` target + image-size validation.
4. **Phase 10.1 reference manifests** — six remaining runtime targets.
5. **Phase 11.4 ack_n DML wiring** — `enforce_commit_policy` adoption in HTTP / gRPC / wire DML handlers.
6. **Phase 12 perf program** — gated on the official duel run; only the worst measured deltas get attacked, no speculative work.
