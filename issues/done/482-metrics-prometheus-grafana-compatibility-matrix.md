# Metrics: Prometheus/Grafana compatibility matrix [DONE]

Labels: research, needs-triage

> *This was generated by AI during triage.*

## HITL instruction

This issue required human review before implementation. The compatibility line
determines what RedDB promises to Prometheus, Grafana, and migration customers
in v0.

## Parent

issues/prd/reddb-metrics-backend-v0.md

## Resolution

The v0 compatibility matrix is now documented in:

- docs/operations/metrics-compatibility.md

The matrix defines RedDB's v0 contract for:

- Prometheus remote-write ingest.
- Prometheus HTTP query API endpoints needed by Grafana.
- The supported PromQL subset.
- Classic histogram behavior for p50, p95, and p99 dashboards.
- Explicit out-of-scope Prometheus/Grafana behavior.
- Required remote-write and Grafana fixtures for implementation tests.
- Unsupported PromQL error behavior.
- Grafana smoke-dashboard acceptance.

Human gate result: approved for implementation to proceed using this matrix as
the v0 contract.

## Acceptance criteria

- [x] Prometheus and Grafana upstreams are cloned or referenced under ignored `.study/upstream/` paths with pinned tags/commits.
- [x] A compatibility matrix documents supported endpoints, request/response shapes, PromQL functions/operators, histogram support, and explicit out-of-scope behavior.
- [x] Real remote-write and Grafana query fixtures are identified for downstream implementation tests.
- [x] The matrix states how unsupported PromQL must fail and which Grafana dashboard patterns are expected to work in v0.
- [x] The matrix is reviewed by a human before implementation issues depend on it.

## Blocked by

None.
