{{- include "reddb.validateMode" . -}}
{{- if eq .Values.mode "primary-replica" -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "reddb.replica.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "reddb.labels" . | nindent 4 }}
    app.kubernetes.io/component: replica
spec:
  serviceName: {{ include "reddb.replica.headless" . }}
  replicas: {{ .Values.replica.replicaCount }}
  podManagementPolicy: Parallel
  updateStrategy:
    {{- toYaml .Values.replica.updateStrategy | nindent 4 }}
  selector:
    matchLabels:
      {{- include "reddb.replica.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "reddb.replica.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        checksum/auth: {{ include (print $.Template.BasePath "/secret-auth.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "reddb.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.replica.terminationGracePeriodSeconds }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.replica.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      {{- with .Values.replica.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.replica.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      affinity:
        {{- if .Values.replica.affinity }}
        {{- toYaml .Values.replica.affinity | nindent 8 }}
        {{- else }}
        {{- include "reddb.replica.defaultAffinity" . | nindent 8 }}
        {{- end }}
      {{- with .Values.replica.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-primary
          image: {{ include "reddb.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          command:
            - /usr/local/bin/red
            - health
            - --http
            - --bind
            - {{ include "reddb.primary.fullname" . }}:{{ .Values.primary.service.httpPort }}
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
      containers:
        - name: reddb
          image: {{ include "reddb.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          args:
            - replica
            - --primary-addr
            - http://{{ include "reddb.primary.fullname" . }}:{{ .Values.primary.service.grpcPort }}
            - --path
            - /data/data.rdb
            - --grpc-bind
            - 0.0.0.0:50051
            - --http-bind
            - 0.0.0.0:8080
            {{- if .Values.http.tls.enabled }}
            - --http-tls-bind
            - {{ .Values.http.tls.bindAddr | quote }}
            - --http-tls-cert
            - {{ printf "%s/%s" .Values.http.tls.mountPath .Values.http.tls.certKey | quote }}
            - --http-tls-key
            - {{ printf "%s/%s" .Values.http.tls.mountPath .Values.http.tls.keyKey | quote }}
            {{- if .Values.http.tls.clientCa.secretName }}
            - --http-tls-client-ca
            - {{ printf "%s/%s" .Values.http.tls.clientCa.mountPath .Values.http.tls.clientCa.key | quote }}
            {{- end }}
            {{- end }}
            {{- if .Values.config.vault }}
            - --vault
            - "true"
            {{- end }}
            {{- with .Values.config.extraArgs }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          env:
            {{- include "reddb.commonEnv" . | nindent 12 }}
            - name: REDDB_PRIMARY_ADDR
              value: http://{{ include "reddb.primary.fullname" . }}:{{ .Values.primary.service.grpcPort }}
            {{- if .Values.grpc.tls.enabled }}
            - name: REDDB_GRPC_TLS_BIND
              value: {{ .Values.grpc.tls.bindAddr | quote }}
            - name: REDDB_GRPC_TLS_CERT
              value: {{ printf "%s/%s" .Values.grpc.tls.mountPath .Values.grpc.tls.certKey | quote }}
            - name: REDDB_GRPC_TLS_KEY
              value: {{ printf "%s/%s" .Values.grpc.tls.mountPath .Values.grpc.tls.keyKey | quote }}
            {{- if .Values.grpc.tls.clientCa.secretName }}
            - name: REDDB_GRPC_TLS_CLIENT_CA
              value: {{ printf "%s/%s" .Values.grpc.tls.clientCa.mountPath .Values.grpc.tls.clientCa.key | quote }}
            {{- end }}
            {{- end }}
          ports:
            - name: grpc
              containerPort: 50051
              protocol: TCP
            {{- if .Values.grpc.tls.enabled }}
            - name: grpc-tls
              containerPort: {{ .Values.replica.service.grpcTlsPort | default 50052 }}
              protocol: TCP
            {{- end }}
            - name: http
              containerPort: 8080
              protocol: TCP
            {{- if .Values.http.tls.enabled }}
            - name: https
              containerPort: {{ .Values.replica.service.httpTlsPort | default 8443 }}
              protocol: TCP
            {{- end }}
          {{- include "reddb.probes" . | nindent 10 }}
          resources:
            {{- toYaml .Values.replica.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
            {{- if .Values.grpc.tls.enabled }}
            - name: grpc-tls
              mountPath: {{ .Values.grpc.tls.mountPath }}
              readOnly: true
            {{- if .Values.grpc.tls.clientCa.secretName }}
            - name: grpc-tls-client-ca
              mountPath: {{ .Values.grpc.tls.clientCa.mountPath }}
              readOnly: true
            {{- end }}
            {{- end }}
            {{- if .Values.http.tls.enabled }}
            - name: http-tls
              mountPath: {{ .Values.http.tls.mountPath }}
              readOnly: true
            {{- if .Values.http.tls.clientCa.secretName }}
            - name: http-tls-client-ca
              mountPath: {{ .Values.http.tls.clientCa.mountPath }}
              readOnly: true
            {{- end }}
            {{- end }}
            {{- include "reddb.vaultVolumeMount" . | nindent 12 }}
            {{- include "reddb.extraSecretVolumeMounts" . | nindent 12 }}
      volumes:
        - name: tmp
          emptyDir: {}
        {{- if not .Values.replica.persistence.enabled }}
        - name: data
          emptyDir: {}
        {{- end }}
        {{- if .Values.grpc.tls.enabled }}
        - name: grpc-tls
          secret:
            secretName: {{ required "grpc.tls.secretName is required when grpc.tls.enabled" .Values.grpc.tls.secretName }}
            defaultMode: 0o400
        {{- if .Values.grpc.tls.clientCa.secretName }}
        - name: grpc-tls-client-ca
          secret:
            secretName: {{ .Values.grpc.tls.clientCa.secretName }}
            defaultMode: 0o444
        {{- end }}
        {{- end }}
        {{- if .Values.http.tls.enabled }}
        - name: http-tls
          secret:
            secretName: {{ required "http.tls.secretName is required when http.tls.enabled" .Values.http.tls.secretName }}
            defaultMode: 0o400
        {{- if .Values.http.tls.clientCa.secretName }}
        - name: http-tls-client-ca
          secret:
            secretName: {{ .Values.http.tls.clientCa.secretName }}
            defaultMode: 0o444
        {{- end }}
        {{- end }}
        {{- include "reddb.vaultVolume" . | nindent 8 }}
        {{- include "reddb.extraSecretVolumes" . | nindent 8 }}
  {{- if .Values.replica.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          {{- include "reddb.replica.selectorLabels" . | nindent 10 }}
        {{- with .Values.replica.persistence.annotations }}
        annotations:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      spec:
        accessModes:
          {{- toYaml .Values.replica.persistence.accessModes | nindent 10 }}
        resources:
          requests:
            storage: {{ .Values.replica.persistence.size | quote }}
        {{- if .Values.replica.persistence.storageClass }}
        storageClassName: {{ .Values.replica.persistence.storageClass | quote }}
        {{- end }}
  {{- end }}
{{- end }}
