# Data model — RedDB Domain Glossary

Part of the [glossary map](../CONTEXT-MAP.md). How data is shaped and accessed, orthogonal to deployment shape: query & streams, catalog/discovery, keyed collection models, events, queues, and analytics.

## Query

- **Logical type system** — the `Value`/`DataType`/`SqlTypeName` vocabulary plus coercion rules (today in `reddb-server/src/storage/schema/types.rs`, referenced ~180+ times across every subsystem). Decided 2026-06-09: moves to a **neutral keystone crate `reddb-io-types`** so that `reddb-io-file`, `reddb-io-wire`, the planned `reddb-io-rql`, and the planned `reddb-io-crypto` can all depend on it without cycles and without any authority crate depending back on the server. Distinct from the *physical* type encoding (`PhysicalSqlTypeName`, `BTreeValueCell`, `ValueFlag`), which stays in `reddb-io-file`.
- **RQL** — RedDB's query language surface. Not just SQL: one hand-rolled engine (zero external parser deps) accepting seven modes (SQL, Gremlin, Cypher, SPARQL, Path, Natural, vector extensions) where every mode is a thin translator (~3.5k LOC total) into the shared SQL AST, then one analyzer→optimizer pipeline.
- **`reddb-io-rql` boundary** — decided 2026-06-09: the crate owns the **language front-end** (lexer → parser → AST → mode translators → analyzer → expr typing → optimizer; ~27k LOC: text in, typed logical plan out) **plus the execution conformance suite** (sqllogictest-style golden queries + expected results, run against the server engine in CI). The physical executors (~20k LOC: `executors/`, `batch/` SIMD, `engine/`) stay in `reddb-server` — but their *correctness specification* lives in the rql crate's conformance suite. Coverage posture (decided 2026-06-09): **aspirational ~95%+ measured by `cargo llvm-cov`, reported in CI, no hard merge gate.** All seven mode translators are pure (AST + `Value` only, no AI deps) and move with the crate.
- **Parser evaluation pyramid** — decided 2026-06-09, the full stack: (1) **proptest round-trip** — generated ASTs printed to SQL and re-parsed must be identical (structural correctness over an infinite query space); (2) **cargo-fuzz no-panic** — arbitrary input may only ever return clean `Err`, never panic/hang (bounded CI job + longer nightly); (3) **corpus golden tests** — real queries with AST snapshots *and error-message-with-position snapshots* (error quality is half the query UX); (4) **differential vs `sqlparser-rs`** on the SQL mode — accept/reject divergence flags unintentional dialect drift. Coverage % emerges from the pyramid, not from tests written to inflate the number.
- **Conformance truth source** — decided 2026-06-09: adopt the **sqllogictest format** (mature Rust harness crate, as used by DataFusion/RisingWave) and run the **public SQLite corpus** over the SQL subset RedDB supports — externally verified expected results at scale. RedDB-only surfaces (vector, graph/Gremlin/Cypher/SPARQL/Path modes, RedDB extensions) get hand-authored goldens; engine-output characterization snapshots are allowed as a cheap *regression* layer there while hand-verified goldens grow, but characterization is never promoted to "truth".
- **RQL extraction sequencing** — decided 2026-06-09, three phases, each its own PRD → `/to-issues` → AFK drain, queued **after** the current drain empties (`#1051`/`#1052` touch the same storage area): *Phase 1* — extract `reddb-io-types` as a byte-faithful re-home with a `pub use reddb_io_types::*` re-export shim at `storage::schema` (the ~180 call-sites stay untouched); *Phase 2* — build the conformance suite against the **current** code first (pin behavior before the move), then extract the front-end into `reddb-io-rql`, suite-green proving the move changed nothing; *Phase 3* — full parser pyramid + llvm-cov reporting in CI.
- **JSON literal** — inline strict-JSON value written directly in RQL, no surrounding quotes: `({"level": "info"})`. The only way to write JSON values in queries (decided 2026-07-03: the quoted-string coercion `'{…}'` is rejected with a didactic error, no deprecation window). Strict JSON only — relaxed `{key: 'v'}` stays reserved for the Cypher property bag. For string→JSON at runtime, use `JSON_PARSE`.
- **`JSON_PARSE`** — explicit string→JSON conversion function for expression positions where the value is already a string at runtime (stringly column, computed expression). Programmatic complement to the JSON literal; never the taught happy path (decided 2026-07-03).
- **Array literal typing** — a bare `[…]` literal parses lossless (integers stay integers); whether it becomes a vector or a JSON array is resolved by the analyzer/runtime from the target's type, never at parse time (decided 2026-07-03 — parse-time f32 commitment silently corrupted large integers).
- **Lossless JSON numbers** — decided 2026-07-05: the in-house JSON value type preserves numeric values exactly — integers stay exact integers, and numbers beyond native ranges survive via a decimal-text representation; f64 is one representation among others, never the ceiling. Closes the gap where exact big-ints survived as native `Value::Array` (KV/row) but not through the document body (#1708). Lands as a clean break (no compatibility window). The LTAP-style *overflow field* (canonical text carried in a structured sidecar) is reserved for boundaries RedDB does not own — e.g. a future Parquet export — and is never used internally.
- **Model marker** — DML keyword naming a collection model (`DOCUMENT`, `NODES`, `EDGES`, …). Rule (decided 2026-07-03): a marker exists only where it disambiguates what the catalog cannot know. In INSERT it is an optional **model assertion** — creates the collection with that model if absent, validates if present (idempotent bootstrap). In UPDATE, `DOCUMENTS`/`ROWS`/`KV` are removed (the catalog knows the model; dotted SET paths resolve via analyzer); `NODES`/`EDGES` stay — a graph collection holds both record kinds, so only the user can say which.
- **Identifier/data case rule** — SQL identifiers fold case (SQL convention); JSON body keys never fold — they are user data, matched exactly everywhere (decided 2026-07-03). A schemaless field miss returns absent, not an error; the typo hazard is a documentation concern, not a semantics lever.
- **EXPLAIN family** — `EXPLAIN` extends beyond SELECT to mutating statements, following the existing `EXPLAIN ALTER` / `EXPLAIN MIGRATION` precedent (decided 2026-07-05). Two tiers: `EXPLAIN <dml>` = plan only, nothing executes; `EXPLAIN ANALYZE <dml>` = executes inside an SI transaction that **always aborts**, reporting real affected counts. Invariant: **RedDB never commits under EXPLAIN** — a deliberate, pinned divergence from Postgres (where `EXPLAIN ANALYZE` on DML commits). There is no separate `DRY RUN` verb. Depends on effects (subscriptions, queue emissions, triggers) firing only at commit.
- **Statement frame** — the per-query lifecycle wrapper (`runtime/statement_frame.rs`) that owns parsing, scope resolution, execution dispatch, result-cache decision, and timing metadata. Single hop every query crosses, regardless of transport.
- **Result cache** — SQL result-row cache with three backends (`Legacy`, `BlobCache`, `Shadow`). Selected via `runtime.result_cache.backend` config knob.
- **AggregateQueryPlanner** — push-down GROUP BY planner that materializes O(group count) instead of O(row count).
- **AskPipeline** — 4-stage funnel for the AI ASK command: token extraction → schema vocabulary match → vector search scoped → value filter. Stage 1 is opt-in heuristic-or-LLM via `ai.ner.backend` config. Under the ASK redesign (decided 2026-07-04) the funnel becomes the planner's grounding stage: it narrows the schema slice *before* any LLM sees it, so a 1000-collection catalog never reaches the model raw.
- **Ask planner** — LLM planning step of the redesigned ASK (decided 2026-07-04, supersedes ADR 0013 §15 single-shot): receives the funnel-narrowed schema slice, self-critiques whether that grounding sustains the question (at most 1 `refine_retrieval` retry), routes the intent, and emits a typed plan validated by the production parser. Step budget capped by `ask.max_plan_steps` (default ~3), per-query `STEPS N` never exceeds the config cap.
- **Ask intent** — the three question classes the planner routes: **factual** (generate read-only RQL over the full read-only surface — joins, aggregations, global `any` — auto-execute, synthesize a cited answer over the returned rows), **synthesis** ("summarise/explain/correlate" — retrieval-RAG with validated `[^N]` citations, as today), and **how-to** (meta-question about the database — answer carries suggested RQL/SQL, including mutating/DDL statements, which are returned but never executed).
- **Ask auto-execution rule** — decided 2026-07-04: read-only generated queries execute by default (`EXECUTE` and `AS RQL` clauses die, clean break); a plan/query-only parameter returns the plan and candidate query without executing. Mutating candidates are never auto-executed under any flag — they can only travel as suggestions.
- **Ask suggestion** — structured envelope field for the how-to intent: a list of parser-validated statements each flagged `mutating: true|false`, plus rationale, alongside the natural-language `answer`. ASK itself stays free of write side-effects; materializing a suggestion as a migration artifact is a separate future command consuming this envelope.
- **Output stream** — bidirectional result-set delivery for a single query, pinned to an MVCC snapshot at open and bounded by `stream.snapshot.ttl_ms`. Distinct from durable-stream/notification/queue (ADR 0028) — carries query results, not events. See ADR 0029.
- **Input stream** — bulk-write counterpart that accepts rows piped from the client and auto-commits per chunk; partial failure leaves a recoverable RID prefix. Not a transaction — explicit `BEGIN ... COMMIT` is incompatible and rejected at open. See ADR 0029.
- **Stream lease** — internal, unforwarded credential issued at `OpenStream`, bound to the snapshot pin. Outlives the bearer token so credential rotation does not terminate accepted streaming work; lease TTL is capped by snapshot TTL.
- **Chunk page alignment** — output-stream production buffer reads in N × 16 KiB units (engine `PAGE_SIZE`). Server flushes when byte, row, or latency cap fires. Wire frames carry row-encoded data, not raw page bytes — drivers stay thin and storage layout stays free to evolve.
- **Resumable annotation** — output-stream open returns `resumable: true|false` based on whether the query has a stable total order. Resume re-executes against the pinned snapshot with `resume_after_rid` filter; prefix hash detects substitution.

## Catalog & Discovery

- **Collection** — root container in RedDB. Every persisted dataset is a Collection regardless of model. The `model` discriminator narrows to `table`, `document`, `queue`, `vector`, `graph`, `timeseries`, or `kv`. Name resolution, ACLs, and storage segments all happen at Collection granularity.
- **Envelope fields** — system-owned attributes present on every returned record regardless of model: `rid`, `collection`, `kind`, `tenant`, `created_at`, `updated_at`. They are RedDB base vocabulary and double as the `FROM ANY` projection columns. Never prefixed or renamed.
- **Reserved-field rule** — envelope field names are reserved at the top level of user data in every model; a colliding user field (e.g. a document body with top-level `kind` or `created_at`) is rejected at write time and the user renames their data. Deliberate divergence from the Mongo/CouchDB/ES convention of underscore-prefixed system fields, keeping one unprefixed vocabulary across models (decided 2026-07-03). Exception: declared tables with timestamps enabled treat `created_at`/`updated_at` as the system columns.
- **CollectionDescriptor** — canonical metadata struct (`catalog.rs:33`) emitted by the catalog snapshot: name, model, schema_mode, entities count, segments count, indices, attention flags. Source of truth for any introspection surface (HTTP `/catalog`, SQL `SHOW`, Postgres-wire `pg_class`).
- **`SHOW COLLECTIONS`** — canonical SQL/RQL discovery command. Lists every Collection regardless of model. **Not yet implemented** as of 2026-05-08; today only `GET /catalog` HTTP exposes the snapshot.
- **`SHOW TABLES` / `SHOW GRAPHS` / `SHOW QUEUES` / `SHOW VECTORS` / `SHOW DOCUMENTS` / `SHOW TIMESERIES` / `SHOW KV`** — typed filters over `SHOW COLLECTIONS`. Each returns only Collections whose `model` matches the keyword. **Not yet implemented**. Faithful-to-type rule: a user asking "show me my tables" should not see queues mixed in.
- **`red` schema** — reserved schema namespace for RedDB-native virtual tables exposing engine introspection: `red.collections`, `red.indices`, `red.stats`, etc. The `red.*` prefix is the canonical RedDB-native form; `pg_catalog.*` views (`pg_class`, `pg_tables`, `pg_indexes`) layer Postgres-wire compatibility on top by translating column shape. Distinct from the column-level `red_*` prefix used for synthetic fields like `red_entity_id` and `red_capabilities`.
- **`SHOW COLLECTIONS` desugaring** — parser-level macro: `SHOW COLLECTIONS` → `SELECT name, model, schema_mode, entities, segments, indices, in_memory_bytes, on_disk_bytes, attention FROM red.collections`. Typed variants apply a `WHERE model = '<kind>'` filter.
- **Wire adapter** — each non-native wire (Postgres, future MySQL/Mongo) is a translation layer in its own `wire/<protocol>/` module. The engine speaks **only** RedDB-native concepts (`red.collections`, `SHOW COLLECTIONS`, etc). Adapters interpret incoming protocol-specific introspection (PG `pg_class`/`pg_attribute`, Mongo `listCollections`) and rewrite to the native form before the query reaches the engine. Postgres-specific concepts like `relkind`, OIDs, and `attnotnull` live exclusively in `wire/postgres/translator.rs`. See ADR 0010.
- **Internal collection** — `CollectionDescriptor.internal: bool` flag (to be added) hiding system-managed collections (DLQs declared via `WITH DLQ`, audit_log, auto-policy artifacts) from default `SHOW COLLECTIONS` output. `SHOW COLLECTIONS INCLUDING INTERNAL` reveals them. Tenant filtering still applies — internal collections are scoped, not invisible.
- **`red.*` read access** — universally readable by any authenticated principal *within their `EffectiveScope`*. No capability check on read; tenant filtering is mandatory and enforced by the engine, not by user-defined policies. Write/update on `red.*` is gated by `cluster:admin`. See ADR 0011 §read access.
- **`SHOW STATS [FOR] <collection>`** — on-demand profiling of a Collection's contents, any model. Desugars (same idiom as `SHOW COLLECTIONS`) to `SELECT ... FROM red.stats WHERE collection = '<name>'`. Long-format shape — `collection`, `entity` (column / top-level body key / label / namespace), `metric`, `value` — so all seven models share one output contract with model-appropriate metrics (tables: null/distinct/top-values per column; documents: promoted columns plus top-level body-key coverage; KV/graph/vector/queue/timeseries: model-appropriate counts and distributions). First slice computes by scan; the columnar analytics projection (ADR 0069) is a planned fast-path, not a prerequisite.
- **Catalog snapshot freshness** — `red.*` columns split into two consistency tiers: hot fields (`name`, `model`, `entities`, `segments`, `attention`, `in_memory_bytes`) read directly from the live catalog snapshot every query (sub-ms). Cold fields requiring B-tree walks (`on_disk_bytes`) cache 30s per-collection. Read-after-write within the same node is strong; cross-node in clusters is eventually consistent. `red.stats` sits in a third, **computed** tier: reading it triggers on-demand computation (a profiling scan), never a cached snapshot.

## Keyed Collection Models

- **KV** — keyed Collection model for volatile or high-churn application data. Normal KV is the only keyed model that supports Redis-flavor operations such as TTL/EXPIRE, INCR/DECR, ADD/merge, destructive tag invalidation, and physical DELETE semantics.
- **Config** — keyed Collection model for stable operational configuration. Config values are readable as plaintext, may be typed/schema-validated, keep versioned history for rollback, and never support TTL or counter-style mutation.
- **Vault** — keyed Collection model for sealed secrets. Vault values are encrypted before WAL/page/snapshot persistence, `GET VAULT` returns redacted metadata only, and plaintext reads require explicit `UNSEAL` plus audit.
- **SecretRef** — Config value that points at a Vault secret. `GET CONFIG` returns the reference, not plaintext; resolving it is an explicit operation that also requires Vault unseal permission.
- **Unseal** — privileged plaintext read from Vault. Every unseal attempt is audited and is distinct from metadata reads.
- **Rotate** — versioned replacement operation for Config and Vault. Used for safe rollout/rollback of stable settings and secrets.
- **Purge** — privileged irreversible removal of Config/Vault history. Normal `DELETE` on Config/Vault creates a tombstone version instead.
- **`red.config` / `red.vault`** — bootstrap-created system collections for engine-managed configuration and secrets. Legacy pseudo-paths such as `$config.*`, `$secret.*`, and `red.secret.*` normalize to these explicit system collections; they are not the canonical public model.

## Events & Subscriptions

- **Event-enabled collection** — a Collection (table/document/vector/graph/timeseries/kv) declared with `WITH EVENTS [TO <queue>] [REDACT (...)] [WHERE ...]`. Mutations to it emit events to a queue. Queues themselves cannot emit events (loop prevention).
- **Auto-event queue** — when `WITH EVENTS` omits `TO`, engine auto-creates queue `<collection>_events` with mode `FANOUT`. Visible in `SHOW COLLECTIONS` (not internal).
- **Event payload** — JSON envelope: `op` (insert/update/delete/truncate/drop), `collection`, `id` (PK if declared, else synthetic), `ts`, `lsn`, `tenant`, `before`, `after`. Per-collection ordered by `lsn`. `event_id = BLAKE3(collection || id || lsn || op)` for idempotency.
- **Current event durability gap** — `WITH EVENTS` currently writes the source mutation and the queue message as separate store WAL batches in autocommit mode. This creates a crash window where the row can be durable without its event. See `.red/adr/0015-events-dual-write-window.md`; the intended fix is a true internal outbox or same-batch queue write.
- **REDACT clause** — subscription-level field redaction at producer time. `WITH EVENTS TO audit REDACT (email, phone)` strips those fields from `before`/`after` before enqueueing. Engine warns (not errors) when source has `DENY select` policies on columns not covered by REDACT.
- **Subscription** — `(source_collection, target_queue, operations, filter, redact)` tuple persisted in catalog. Multiple subscriptions per collection allowed. Tenant-scoped by default; cross-tenant requires `events:cluster_subscribe` capability. Creating one needs `select` on source + `write` on target queue.
- **EVENTS BACKFILL** — operator command `EVENTS BACKFILL <collection> [WHERE ...] TO <queue> [LIMIT N]` enqueues synthetic-flagged events for existing rows. Idempotent via deterministic `event_id`. Default subscription has no backfill — only future mutations.
- **Synthetic event** — event with `synthetic: true` produced by BACKFILL. Distinct from real-time events so consumers can choose to ignore historical payloads.
- **Live queue wait** — a blocking or long-polling read over an existing queue that wakes when a message becomes deliverable but preserves queue semantics: delivery state, ACK/NACK, DLQ, tenant scope, and authorization. The canonical contract lives in the query surface; transports may adapt it but must not invent hidden driver-only semantics. First-cut wake is strong within one runtime node for commits that make messages deliverable on the target queue; cross-node wake is a later cluster concern. First-cut `WAIT` is autocommit-only, is rejected inside explicit transactions, applies only to `QUEUE READ`, requires an explicit duration, and returns a normal empty result on timeout. Shutdown or connection cancellation returns an explicit cancellation error rather than an empty timeout result. The server owns a maximum wait cap; requests above the cap fail instead of being silently shortened. When many waiters block on one queue, the first cut wakes all local waiters for that queue and lets normal delivery arbitration decide winners. Telemetry is Prometheus counters plus a wait-duration histogram with developer tracing; normal waits, timeouts, and cancellations are not audit/operator events.
- **Ephemeral notification** — a lightweight pub/sub signal with no replay, ACK, consumer offset, or DLQ. Offline listeners miss it; use a queue or stream when durability matters. The RedDB-native contract is canonical; Postgres-like `LISTEN`/`NOTIFY` forms are compatibility translations rather than engine concepts. Channels are tenant-scoped by default; cross-tenant or global notification requires explicit capability.
- **Durable stream** — a Collection model for an append-only event log with per-consumer offsets and stream-specific retention. Unlike a queue, reading does not create pending delivery state; unlike an ephemeral notification, a consumer can resume from its saved offset. First use is app-level publish/read; the event shape should remain compatible with future materialized CDC.

## Coordination

- **Resource reservation** — a transactional claim over finite user-domain capacity, such as stock, seats, slots, or quotas, whose availability and reservation state must share one authoritative commit boundary.
- **Concurrent claim** — a state-changing coordination primitive where competing transactions acquire and update different eligible records instead of serializing behind the same candidate; queues participate through **Queue delivery** semantics rather than a parallel claim path.
- **Claim lock** — a transaction-scoped record lock used by **Concurrent claim** to make an in-flight candidate temporarily unavailable to other claimers without storing lease fields in user data; ordinary DML keeps the normal MVCC conflict policy.
- **Claim identity** — the model-owned identity used by **Claim lock**: table/document records use stable logical identity, KV uses key identity, and queues use **Queue delivery** identity.
- **Exact claim** — a **Concurrent claim** mode that commits only if the requested claim cardinality is fully satisfied by immediately claimable records.
- **Claim miss** — a normal **Concurrent claim** result where the requested claim cardinality cannot be satisfied immediately and the statement applies no writes.
- **Claim authority** — the write authority allowed to decide a **Concurrent claim**; replicas replay or route the resulting write instead of choosing claim winners locally.
- **Owner-local claim** — a **Concurrent claim** restricted to one write owner or range owner; cross-owner exactness is outside the first claim contract.
- **Reservation idempotency key** — an application-defined key stored transactionally with a **Resource reservation** so retried requests observe the existing outcome instead of creating a duplicate claim.

## Queue modes

- **`FANOUT` queue** — every consumer (or consumer group) receives every message. Equivalent to publishing across multiple Kafka consumer groups or RabbitMQ fanout exchange. Default mode for auto-event queues.
- **`WORK` queue** — consumers compete for messages; each message delivered to one consumer in the group. Equivalent to RabbitMQ work queue or Pulsar Shared subscription. Default for `CREATE QUEUE` without explicit mode.
- **Queue delivery** — the lifecycle step that selects an available queue message for a consumer group, records it as pending, and updates attempt counters according to the queue mode.
- **Ordering key** — an optional per-message identity (`QUEUE PUSH ... KEY 'x'`) that serializes delivery among messages sharing the key: at most one pending delivery per `(consumer group, ordering key)`, in both `WORK` and `FANOUT` modes; keyless messages stay unordered. The guarantee is strict under failure — a NACKed/retrying message blocks its key for that group until it is ACKed or retired to the DLQ (DLQ promotion unblocks the key). Mutually exclusive per push with `DELAY`/`AT` and with `PRIORITY` queues, which reorder by definition. The key travels with the message into the DLQ and through `QUEUE MOVE`; a replayed message joins the tail of its key at the destination, never its original position. Ordering is a property of grouped delivery only — raw deque primitives (`QUEUE POP`, either side) sit outside the guarantee, and mixing them with ordered consumption on one queue is a documented user error. Distinct from a consumer group and from any producer dedup identity.
- **Push idempotency key** — an optional per-message producer identity (`QUEUE PUSH ... DEDUP 'id'`) that makes retried pushes idempotent within a queue-scoped dedup window: a duplicate push inside the window is a no-op returning the original message id, whether or not the original was already consumed. The window is a queue property with an engine default (`DEDUP_WINDOW` on `CREATE`/`ALTER QUEUE`); pushes never carry window durations. The key is a first-committer-wins write — concurrent transactions pushing the same key conflict at commit, and the loser's retry observes the committed outcome. The guarantee is durable: it survives restart within the window. The index is written and consulted only at the producer `QUEUE PUSH` boundary — DLQ promotion, retry re-timing, and operator `QUEUE MOVE` bypass it, so a replay can never silently no-op as a duplicate of its own original push. Sibling of **Reservation idempotency key**; orthogonal to and freely combinable with the **Ordering key**.
- **Delayed queue message** — a queue message with a future availability time. It remains durable and inspectable before that time through queue inspection surfaces such as `QUEUE PEEK` and `SELECT ... FROM QUEUE`, but is not deliverable by `QUEUE READ`, `QUEUE POP`, or live queue wait until its availability deadline is reached. This is distinct from queue TTL, which controls expiration rather than first delivery.
- **Pending delivery** — a queue message copy that has been delivered to one consumer group but has not yet been ACKed, NACKed, claimed by another consumer, or retired.
- **Queue retirement** — the lifecycle step that ends a pending delivery by acknowledging it for one group, moving it to a DLQ, dropping it, or physically deleting it when queue mode semantics allow.
- **Queue retry policy** — the configured rule that turns a failed delivery into a future delayed queue message, a DLQ promotion, or a drop. It owns retry delay/backoff decisions; schedulers create new work, while retry policy re-times existing failed work. The queue owns the default policy; an authorized `NACK` may request a per-failure delay override.
- **QueueLifecycle** — deep Module owning the full delivery + retirement state machine for both `WORK` and `FANOUT` queues: pick → lock → ack/nack/drop/DLQ. Replaces today's split between `runtime/impl_queue.rs` and `runtime/queue_delivery.rs`. Participates in the caller's transaction (statement frame), reclaims expired locks lazily on next `deliver()`, owns retry/DLQ policy read from queue catalog metadata. Depends on a narrow `QueueStore` adapter trait (not `&Engine`) — primary executes decisions; replicas replay WAL outcomes via the Logical change applier.

## Analytics

- **Analytics** — product/domain umbrella for RedDB-native analytical capabilities. It organizes concrete user-facing concepts such as metrics, KPIs, cohorts, funnels, materializations, and approximate aggregates; it is not itself a vague public object type like `CREATE ANALYTICS ...`. Public APIs should prefer concrete domain objects such as metrics, KPIs, cohorts, and funnels. Analytics catalog/data operations should use RedDB's ordinary `SELECT`, `INSERT`, and `UPDATE` surfaces, participate in WAL durability like other persisted data, and integrate with existing policy/access-control semantics rather than introducing separate `SHOW`/`GRANT`-style operations.
- **Metric descriptor** — WAL-backed analytics catalog record keyed by a stable dotted metric path (for example `infra.database.cpu.usage`) and carrying the metric type/kind (`counter`, `gauge`, `histogram`), unit, dimensions, retention, rollup, source/query, role, and cardinality policy. `CREATE METRIC <path> TYPE <kind>` / `UPDATE METRIC <path>` may exist as ergonomic sugar, but the canonical state is an ordinary selectable/updatable catalog record (for example under `red.analytics.metrics`) governed by RedDB policies. The system should support both direct catalog access for integration/debug/policies and typed UX sugar such as `SELECT ... FROM METRICS <prefix>`.
- **Metric dotted path** — stable metric taxonomy, not a hidden dimension store. Use the path for what is being measured (`infra.database.cpu.usage`) and dimensions for varying instances (`host`, `region`, `core`, `tenant`, `service`). Avoid embedding high-cardinality instance IDs in the path. Metrics should be addressable by prefix for reads/policies/tree views, similar in spirit to KV/config/vault dotted keys, while remaining a metric-specific contract.
- **Metric SQL surface** — prefer typed SQL definitions over separate admin verbs: `CREATE METRIC <path> TYPE <kind>` for descriptor declaration, `UPDATE METRIC <path> SET ...` for descriptor changes, and `SELECT ... FROM METRIC <path>` / `SELECT ... FROM METRICS <prefix>` for reading metric outputs once execution exists. Raw writes should go to ordinary RedDB collections (`TABLE`, `DOCUMENT`, etc.); metrics define how to measure those collections or their materializations. Do not make `INSERT INTO METRIC` part of Analytics v0; direct metric ingest may be considered later only as explicit sugar over a normal sample collection. Analytics v0 should start with WAL-backed, policy-governed metric descriptors/catalog state before attempting derived metric execution/materialization. This keeps `METRIC` explicit enough to avoid KV/table ambiguity while preserving RedDB's ordinary WAL durability and policy integration.
- **Reliability vocabulary** — SLI/SLO/SLA likely belongs as a semantic layer on top of metrics, not as a replacement for metrics. A metric is the measured time-series/derived value; an SLI is a service-quality metric with a precise definition and should be modeled canonically as a metric role (`ROLE sli`); an SLO is a target/window/objective over an SLI and is the first object that should stand separately above metrics; `CREATE SLO <path> ON METRIC <metric_path> TARGET ... WINDOW ...` can exist as ergonomic sugar, but canonical SLO state should be WAL-backed, selectable/updatable, and policy-governed. An SLA is an externally/customer-facing commitment tied to SLOs plus policy/consequence metadata and is likely post-MVP.
- **Ranking** — order-statistics over a scored Collection: answering "what position/percentile is row X?" rather than just "give me the top N". Top-N is already served by a sorted index + `ORDER BY score LIMIT`; Ranking is the distinct capability of reporting a row's *position* in that order. It is a capability declared on an ordinary table's score column, not a new Collection model, and its canonical surface is SQL (`RANK() OVER (ORDER BY …)`); Redis-style `Z*` verbs are sugar that desugars to it. A **Leaderboard** is the user-facing application of Ranking. Rank has two consistency tiers (exact, MVCC-correct for a bounded top-K head; approximate for the tail) — see ADR 0035.
- **Events vs metrics vs probabilistic structures** — `EVENT` is not currently assumed to require a new storage model. Prefer treating events as ordinary table/document collections with an analytics source profile/contract (`what happened`, with actor/session/properties/context/order). `METRIC` is the central named analytical object: a measure over time, either direct samples or a query/materialization over table/document/event-shaped sources. TimeSeries is the storage/layout for metric samples/materializations; probabilistic structures are approximate execution sidecars/summaries for specific questions such as unique count, frequency, and membership. Product analytics should derive metrics from event-shaped sources rather than treating events as just metric samples. Cohort/funnel APIs are intentionally deferred in Analytics v0; treat them as future product-analytics definitions that may produce multiple metrics, not as MVP storage primitives.
