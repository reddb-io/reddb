# Governance — RedDB Domain Glossary

Part of the [glossary map](../CONTEXT-MAP.md). Auth & security, telemetry channels, and compliance/evidence — cross-cutting controls that apply regardless of deployment shape.

## Auth & Security

- **User** — the only login principal type in RedDB. Operational, tenant, service-account, and application identities are all users; differences in power come from attached policies, tenant scope, and explicit ownership markers rather than separate account classes.
- **Admin** — conventional high-privilege user shape produced by bootstrap or policies. Admin authority comes from attached policies such as Allow-all and remains subject to explicit Deny statements; Admin is not an authorization bypass.
- **EffectiveScope** — per-request authorization context combining tenant identity + auth principal + capability set. Carried through the statement frame to every authorization check.
- **Capability** — string identifier (e.g., `cluster:topology:read`, `ai:ner:read`) gating an operation. Today the engine has the seam but no real capability checker — placeholder returns `false` for non-trivial capabilities.
- **Platform-scoped user** — user with `tenant_id = None`. Platform scope is an addressability boundary, not a separate account type; access still comes from policies.
- **System-owned user** — operator-owned user with `system_owned = true`, created through the `_admin` surface guarded by `RED_ADMIN_TOKEN`. Destructive user mutations such as delete, disable, password change, and role change are rejected; API key rotation remains allowed. System ownership is not a superuser bypass: managed-surface mutations still require platform scope and an explicit policy/capability for the target action and resource.
- **Managed policy** — operator-owned policy document installed at bootstrap to protect operator-controlled behavior. Customer or tenant admins cannot change Managed policies unless they are also a platform-scoped system-owned principal with the required policy-management capability. A Managed policy is self-described by policy metadata and pinned by an internal integrity registry so removing the metadata from the document cannot unlock it. Managed enforcement is an executor invariant as well as a policy decision: managed targets fail closed before ordinary policy allow can widen them.
- **Bootstrap manifest** — optional first-boot document that declares initial users, managed policies, policy attachments, config preset, and managed config namespaces. Environment variables cover the simple bootstrap path; the manifest exists for advanced setups that need reproducible initial authorization state.
- **Allow-all policy** — bootstrap policy that grants broad database authority to an initial user. Its power is still subject to explicit Deny statements from Managed policies; it is not an authorization bypass.
- **Protect-managed policy** — Managed policy that denies ordinary users from mutating Managed policies, Managed config namespaces, and system-owned users. It is the default guardrail that lets an allow-all user administer everything except operator-owned guardrails.
- **`red.registry`** — protected system registry that stores governance metadata for internal resources such as config keys, vault paths, policies, audit surfaces, and future RedDB-owned surfaces. Values still live in their native stores (`red.config`, `red.vault`, policy store, etc.); `red.registry` records type/schema, mutability, sensitivity, managed status, required policy action/resource, evidence requirements, and preset ownership. Registry mutations happen through explicit governance commands rather than ordinary DML against the registry surface. The active view is versioned; registry history records prior versions for audit and rollback.
- **HeaderEscapeGuard / AuditFieldEscaper / SerializedJsonField / ConnStringSanitizer** — typed guards from PRD #173 enforcing escape-safe construction at HTTP / audit / JSON / connection-string boundaries.
- **Tainted&lt;T&gt;** — wrapper requiring explicit `escape_for(boundary)` re-serialization before a connection-string-derived value crosses any other boundary (log, header, audit, JSON).

## Telemetry

- **Telemetry channel** — one of three logical buckets RedDB emits structured events into: **operator-grade events** (audit log + ops dashboard), **slow query log** (perf-tuning bucket), or **developer signal** (filterable diagnostic noise via `tracing`). Each channel has its own sink, retention policy, and intended audience.
- **OperatorEvent** — closed enum of paging-or-forensic events the operator must notice within minutes (paging) or audit later (forensic). Always-emit, never-sampled, never-filterable. Persisted via the existing AuditLogger (audit log channel) plus a parallel `tracing::warn!(target: "reddb::operator")` breadcrumb for ops dashboards.
- **Slow query log** — separate sink (`red-slow.log`) populated only when query duration exceeds `runtime.log.slow_query_threshold_ms`. Distinct file, distinct rotation policy, distinct retention. Mirror of PG `auto_explain` / `log_min_duration_statement`.
- **AuditLogger** — structured JSON-line emitter with file rotation, secret redaction, and typed fields via `AuditFieldEscaper` (per PRD #177). The sink for operator-grade events.
- **Lossy log writer** — `tracing_appender::NonBlocking` configured with `lossy: true`: hot path pushes onto an MPSC channel; a dedicated worker thread owns the file/stderr fd. On buffer saturation, lines drop rather than backpressure the hot path. Mirror of PG's `logging_collector` subprocess pattern.

## Compliance

- **Compliance Enablement** — RedDB product capability that gives customers controls and audit evidence they can map to external frameworks such as SOC 2, ISO/IEC 27001, HIPAA, PCI DSS, GDPR, and LGPD. It does not mean RedDB automatically makes the customer's organization compliant.
- **Audit Evidence** — durable, attributable record used to answer an auditor's question about who attempted or completed what, when, under which authorization context, and with what outcome. Denied Control Events are evidence too.
- **Control Event** — security- or operations-relevant internal action performed against the RedDB control plane, such as user lifecycle changes, policy changes, tenant/RLS changes, vault unseal, secret rotation, schema changes, backup, restore, failover, or runtime configuration changes.
- **Control Event Ledger** — native append-only evidence surface for Control Events. Allowed, denied, and error outcomes share one canonical ledger so investigators can reconstruct one timeline by actor, resource, and policy decision. It is distinct from query auditing: every Control Event is low-volume and compliance-relevant; query events are high-volume and configurable by scope.
- **Evidence minimization** — Control Events store safe metadata and fingerprints by default rather than raw payloads. Passwords, secret values, tokens, private keys, certificates, and query text containing application data must not become permanent audit payloads unless an explicit redacted/approved evidence policy allows it.
- **Query audit** — optional high-volume evidence stream for data-plane statements. It is separate from the Control Event Ledger and is configured by scope; by default it records metadata such as actor, tenant, statement kind, touched collections, duration, row counts, and query hash rather than raw query text. Regulated presets enable the query-audit infrastructure but do not audit every query globally by default.
- **Control evidence matrix** — capability-first documentation and introspection surface mapping RedDB controls and evidence surfaces to external audit frameworks. It starts from what RedDB can prove, then maps to frameworks; it is not a legal checklist that promises automatic compliance.
- **Compliance Mode** — deployment posture where Control Events must be recorded durably before sensitive control-plane actions succeed. If RedDB cannot persist the required Audit Evidence, the sensitive action fails closed.
- **Config Preset** — named configuration baseline that trades audit durability, security controls, and runtime overhead against deployment simplicity. RedDB defaults to the **Simple** preset for lightweight embedded/local use; RDS-like and regulated deployments must opt into stronger presets explicitly.
- **Simple preset** — default Config Preset optimized for minimum friction and low overhead. It is not marketed as audit-ready or compliance-ready.
- **Managed config namespace** — RedDB-owned config prefix whose mutation is reserved for a platform-scoped system-owned principal according to `red.registry`. Tenant/database owners may read only the exposed subset and cannot weaken operator-controlled audit, preset, storage, backup, tenancy, or isolation settings.
