# Integration (e2e) lane baseline — Issue #974

> **Status: measurement in progress.** This file is replaced by the committed
> baseline (pass/fail counts + failing-test list + remediation scope) the moment
> the run completes. Lane command:
> `cargo nextest run --workspace --locked -E 'kind(test)' --no-fail-fast`

Run launched (see commit timestamp). Results pending.
