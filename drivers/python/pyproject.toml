[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "reddb-python"
version = "0.1.0"
description = "Native Python driver for RedDB (compiled Rust, like psycopg2)"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
